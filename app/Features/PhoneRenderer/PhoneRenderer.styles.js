import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    phone: {
        width: 10,
        height: 16,
        position: 'absolute',
        left: 131,
        bottom: 55,
        zIndex: 2
    },
});

export default styles;